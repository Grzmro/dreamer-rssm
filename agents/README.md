# agents/ — Phase 1+

Empty in Phase 0 (intentional). Coming here in later phases:

- actor and critic trained "in imagination" on the prior `p(z|h)`,
- policy-learning training loop without an encoder (imagination rollouts).
