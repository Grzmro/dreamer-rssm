# models/ — Phase 1+

Empty in Phase 0 (intentional). Coming here in later phases:

- RSSM (Recurrent State-Space Model): prior `p(z|h)`, posterior `q(z|h,e)`, deterministic core (GRU),
- observation encoder (CNN) and decoder,
- prediction heads: reward, continue/discount.
